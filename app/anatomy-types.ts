export type Sex='male'|'female';
export type Structure={parentId?:string;childrenIds?:string[];relatedIds?:string[];contextIds?:string[];modelNote?:string;noticeBySex?:Partial<Record<Sex,string>>;detailOffset?:number[];detailBounds?:number[][];sex:Sex|'both';colors?:number;referenceNote?:string;id:string;name:string;en:string;layer:number;system:string;color:string;description:string;functions:string[];source:string;center:number[];size:number[];positions:number;normals:number;indices:number;vertexCount:number;indexCount:number};
export type ViewerState={diagramOpening:number;diagramTissue:string|null;diagramHidden:string[];diagramOutline:boolean;diagramRugae:boolean;context:boolean;sex:Sex;navigation:'rotate'|'pan';panX:number;panY:number;visible:boolean[];opacity:number[];labels:boolean;mode:'whole'|'explode'|'section'|'diagram';explode:number;cut:number;axis:'x'|'y'|'z';selected:string|null;isolate:boolean;view:'front'|'back'|'side';command:number;zoom:number;focus:number;reset:number};
export const INITIAL:ViewerState={diagramOpening:55,diagramTissue:null,diagramHidden:[],diagramOutline:true,diagramRugae:true,context:true,sex:'male',navigation:'rotate',panX:0,panY:0,visible:[true,true,true],opacity:[72,18,100],labels:true,mode:'whole',explode:55,cut:50,axis:'z',selected:null,isolate:false,view:'front',command:0,zoom:0,focus:0,reset:0};
